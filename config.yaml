model: hermes
provider: openai-compatible
terminal:
  backend: local
  cwd: /app
  timeout: 180
compression:
  enabled: true
  threshold: 0.85
fallback_model:
  provider: openai-compatible
  model: hermes
  base_url: https://ninerouter-server-6anx.onrender.com/v1
  key_env: NINEROUTER_API_KEY
platforms:
  telegram:
    enabled: true
    home_channel:
      platform: telegram
      chat_id: "243531053"
      name: Alireza
      user_id: "243531053"
